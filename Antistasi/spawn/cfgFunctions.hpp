class AS_spawn {

    class server {
        FNC(spawn,initialize);
        FNC(spawn,drop);
        FNC(spawn,toggle);
        FNC(spawn,update);
    };

    class common {
        FNC(spawn,states);
        FNC(spawn,dictionary);
        FNC(spawn,spawns);
        FNC(spawn,get);
        FNC(spawn,set);
        FNC(spawn,add);
        FNC(spawn,remove);
        FNC(spawn,execute);
    };
};
