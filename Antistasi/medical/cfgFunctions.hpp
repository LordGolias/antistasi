class AS_medical {
    class common {
        FNC(medical,getAssignedMedic);
        FNC(medical,canHeal);
        FNC(medical,clearAssignedMedic);
        FNC(medical,effectsLoop);
        FNC(medical,healAction);
        FNC(medical,healUnit);
        FNC(medical,initUnit);
        FNC(medical,isHealed);
        FNC(medical,isUnconscious);
        FNC(medical,medicLoop);
        FNC(medical,setUnconscious);

        FNC(medical\templates,crateMeds);
        FNC(medical\templates,FIAuniformMeds);
        FNC(medical\templates,FIAmedicBackpack);
    };
};
